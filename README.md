# File-Encryption-Decryption-Tool-in-C-
simple file encryption/decryption project built in C++. It allows you to encrypt or decrypt the contents of a text file using a Caesar Cipher-style shift, reading from and writing to files securely.  The tool reads the entire file into memory, applies a character shift to encrypt or reverse it for decryption, and produces a new file with the resul
